# Задачи для специалиста по автоматизации на Python

## 1. **Отсортировать числа в массиве согласно сумме их цифр**

```py
def sort_by_digit_sum(nums: list[int]) -> list[int]:
    """
    Сортирует числа по сумме их цифр

    Сложность по времени: O(N * log N * K)
    N — количество чисел в массиве
    K — макс. количество цифр в числе
    """
    return sorted(nums, key=lambda x: sum(int(dig) for dig in str(abs(x))))
```

## 2. **Найти 2 целых числа в заданном массиве с наименьшей суммой**

```py
import heapq


def two_minimal_integers(nums: list[int]) -> list[int]:
    """
    Находит два целых числа в массиве с наименьшей суммой.

    Сложность по времени: O(n log 2) -> эквивалентно O(n).
    """
    # Функция nsmallest выбирает k наименьших элементов за один проход
    return heapq.nsmallest(2, nums)
```

## 3. **Найти наибольший общий делитель всех чисел в заданном диапазоне [L, R].**

```py
def gcd_of_range(L: int, R: int) -> int:
    """
    Находит наибольший общий делитель всех чисел в диапазоне [L, R].
    
"""
```

## 4. **Найти наименьшую степень 2, равную или больше суммы чисел в заданном массиве.**

```py
def smallest_power_of_two(nums: list[int]) -> int:
    """
    Находит наименьшую степень 2, равную или больше суммы чисел в массиве.
    
"""
```

## 5. **Найти наименьшую степень 2, равную или больше суммы чисел в заданном массиве.**

```py

```

*
## 6. **Даны два строковых представления чисел A и B. Нужно максимизировать A, заменив в нём любую цифру на цифру из B. Каждую цифру B можно использовать только один раз.**

```py
    
```
